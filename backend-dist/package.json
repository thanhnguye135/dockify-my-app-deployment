{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node dist/server.js",
    "build": "esbuild server.js --bundle --platform=node --outfile=dist/server.js --format=cjs --external:express --external:mongoose --external:dotenv --external:body-parser"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "esbuild": "^0.25.8",
    "express": "^4.18.3",
    "mongoose": "^7.6.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
